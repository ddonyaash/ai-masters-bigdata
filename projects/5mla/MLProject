name: 5mla

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      train_path: {type: str}
      model_param1: {type: int, default: 100}
    command: "python train.py --train_path {train_path} --model_param1 {model_param1}"
